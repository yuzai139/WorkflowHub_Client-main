import glightboxMin from "glightbox";

// GLightbox Popup
var lightbox = glightboxMin({
  selector: ".image-popup",
  title: false,
});

// GLightbox Popup

var lightboxDesc = glightboxMin({
  selector: ".image-popup-desc",
});

// GLightbox Video Pop-up Button
var lightboxvideo = glightboxMin({
  selector: ".image-popup-video-button",
  title: false,
});

// GLightbox Video
var lightboxvideo = glightboxMin({
  selector: ".image-popup-video",
  title: false,
});

// GLightbox Iframes and Inline Elements
var lightboxvideo = glightboxMin({
  selector: ".image-iframe-elements",
  title: false,
});
